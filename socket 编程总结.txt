socket 编程总结：

server : 不间断发送数据，发送的数据长度都不一样；

client : 不断的接受数据，接受数据分2步：
						
						1- 先接受头部，确定好长度；
						2- 在接受确定的长度数据；

		好处：不会丢失数据，如果不按照这种做法会丢失数据；
			  因为接受的数据几倍的大于发送的数据时，发过来的多个数据包被一个buf接受，
			  在等待下一个的buf数据。前一个buf的后面长度数据被忽略掉了
			  
